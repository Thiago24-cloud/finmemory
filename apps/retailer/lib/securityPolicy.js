/** @deprecated Import from `@finmemory/shared/validation` — re-export de compatibilidade. */
export {
  validatePasswordStrength,
  normalizeEmail,
  isValidEmail,
} from '@finmemory/shared/validation';

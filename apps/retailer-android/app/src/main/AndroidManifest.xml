<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <!-- Rede (obrigatório para API retailer + Stone) -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <!-- SmartPOS Stone: Bluetooth para periféricos (impressora/leitor) — ajustar por modelo -->
  <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
  <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

  <!-- Consulta ao app de pagamentos Stone na mesma maquininha (SDK, não Deeplink externo) -->
  <queries>
    <package android:name="br.com.stone.posandroid" />
    <package android:name="br.com.stone.posandroid.datacontainer" />
  </queries>

  <application
      android:name=".FinMemoryRetailerApp"
      android:allowBackup="false"
      android:icon="@drawable/ic_launcher_foreground"
      android:label="@string/app_name"
      android:networkSecurityConfig="@xml/network_security_config"
      android:roundIcon="@drawable/ic_launcher_foreground"
      android:supportsRtl="true"
      android:theme="@style/Theme.FinMemoryRetailer"
      android:usesCleartextTraffic="false"
      tools:targetApi="36">

    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:launchMode="singleTask"
        android:screenOrientation="portrait"
        android:windowSoftInputMode="adjustResize">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- Stone SDK: meta-data e providers serão declarados na Fase 1 após dependência AAR -->
    <!--
    <meta-data android:name="stone-code" android:value="${stoneCode}" />
    -->

  </application>
</manifest>

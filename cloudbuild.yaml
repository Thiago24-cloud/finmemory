steps:
  # Build da imagem Docker (Mapbox token via _MAPBOX_ACCESS_TOKEN)
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--build-arg'
      - 'NEXT_PUBLIC_SUPABASE_URL=${_NEXT_PUBLIC_SUPABASE_URL}'
      - '--build-arg'
      - 'NEXT_PUBLIC_SUPABASE_ANON_KEY=${_NEXT_PUBLIC_SUPABASE_ANON_KEY}'
      - '--build-arg'
      - 'NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=${_MAPBOX_ACCESS_TOKEN}'
      - '--build-arg'
      - 'NEXT_PUBLIC_BUILD_ID=${_COMMIT_SHA}'
      - '--build-arg'
      - 'NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=${_NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY}'
      - '-t'
      - 'gcr.io/$PROJECT_ID/finmemory:$_COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/finmemory:latest'
      - '.'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/finmemory:$_COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/finmemory:latest'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'finmemory'
      - '--image'
      - 'gcr.io/$PROJECT_ID/finmemory:$_COMMIT_SHA'
      - '--region'
      - 'southamerica-east1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--memory'
      - '1Gi'
      - '--timeout'
      - '60s'
      - '--max-instances'
      - '10'
      - '--cpu'
      - '1'

images:
  - 'gcr.io/$PROJECT_ID/finmemory:$_COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/finmemory:latest'

# Substitutions: _COMMIT_SHA obrigatório. Supabase/Mapbox públicos vêm do deploy (não commitar valores).
substitutions:
  _COMMIT_SHA: 'manual'
  _NEXT_PUBLIC_SUPABASE_URL: ''
  _NEXT_PUBLIC_SUPABASE_ANON_KEY: ''
  _MAPBOX_ACCESS_TOKEN: ''
  _NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: ''
